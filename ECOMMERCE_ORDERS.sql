CREATE OR REPLACE TABLE TRAINING_SNOWFLAKE_TABLES.RAW_ZONE.ECOMMERCE_ORDERS (
    ORDER_ID            VARCHAR(20),
    CUSTOMER_ID         VARCHAR(20),
    CUSTOMER_NAME       VARCHAR(100),
    CUSTOMER_EMAIL      VARCHAR(150),
    ORDER_DATE          DATE,
    ORDER_TIMESTAMP     TIMESTAMP_NTZ,
    PRODUCT_ID          VARCHAR(20),
    PRODUCT_NAME        VARCHAR(200),
    CATEGORY            VARCHAR(50),
    SUB_CATEGORY        VARCHAR(50),
    QUANTITY            INTEGER,
    UNIT_PRICE          FLOAT,
    DISCOUNT_PCT        INTEGER,
    GROSS_AMOUNT        FLOAT,
    DISCOUNT_AMOUNT     FLOAT,
    TAX_RATE            INTEGER,
    TAX_AMOUNT          FLOAT,
    NET_AMOUNT          FLOAT,
    SHIPPING_COST       FLOAT,
    PAYMENT_METHOD      VARCHAR(30),
    ORDER_STATUS        VARCHAR(20),
    SALES_CHANNEL       VARCHAR(30),
    REGION              VARCHAR(20),
    COUNTRY             VARCHAR(30),
    WAREHOUSE_CODE      VARCHAR(20),
    CUSTOMER_TIER       VARCHAR(20),
    IS_PRIME_MEMBER     VARCHAR(5),
    DELIVERY_DATE       DATE,
    -- Metadata columns
    LOAD_TIMESTAMP      TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP(),
    SOURCE_FILE         VARCHAR(200) DEFAULT 'ecommerce_orders.csv'
)
DATA_RETENTION_TIME_IN_DAYS = 30
COMMENT = 'Raw e-commerce orders - permanent table with 30-day Time Travel';
